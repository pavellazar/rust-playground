// This module contains the shared logic for fetching operations
pub fn fetch_operations() -> Vec<String> {
  // This could be a real service call
  // For simplicity, keeping it as a mock
  vec!["add".to_string(), "mul".to_string()]
}