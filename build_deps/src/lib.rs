// This contains the complex server code for fetching operations
pub fn fetch_operations() -> Vec<String> {
  // Simulate complex server code (could be HTTP, DB, etc.)
  vec!["add".to_string(), "mul".to_string()]
}